include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # @JsonKey pada parameter konstruktor freezed memang dibaca oleh
    # json_serializable; peringatan ini dikenal dan disarankan dimatikan
    # oleh dokumentasi freezed.
    invalid_annotation_target: ignore
